#!/usr/bin/env bash

if output=$(ping -c 3 8.8.8.8 &>/dev/null); then
    python $HOME/scripts/edu && sudo netctl restart eduroam &> /dev/null
else
    sudo netctl restart wlp2s0-Oudedijk3A03 &>/dev/null
fi
