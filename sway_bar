#!/usr/bin/env bash

while (( 1 )); do
    ibus=$(ibus engine 2> /dev/null)
    time=$(date +"%a %Y-%m-%d %H:%M:%S")
    batt=$(batt)
    printf "%s %s" "BATTERY: ${batt} |" "${time}"
    printf "\n"
    sleep 1
done
